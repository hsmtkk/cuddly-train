---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-deploy
  labels:
    app: web-deploy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web-deploy
  template:
    metadata:
      labels:
        app: web-deploy
    spec:
      containers:
        - name: web
          env:
            - name: PORT
              value: 80
          image: asia-northeast1-docker.pkg.dev/cuddly-train/registry/web:latest
          ports:
            - containerPort: 80
      nodeSelector:
        cloud.google.com/gke-spot: "true"
---
apiVersion: v1
kind: Service
metadata:
  name: web-service
spec:
  selector:
    app: web-deploy
  ports:
    - port: 80
      protocol: TCP
